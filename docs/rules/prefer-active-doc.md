# obsidianmd/prefer-active-doc

📝 Prefer `activeDocument` and `activeWindow` over `document` and `window` for popout window compatibility.

💼 This rule is enabled in the following configs: ⚠️ `recommended`, ⚠️ `recommendedWithLocalesEn`.

<!-- end auto-generated rule header -->
